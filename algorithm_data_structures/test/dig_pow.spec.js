let mocha = require('mocha');
let assert = require('assert');
let chai = require('chai').expect

let isIsogram = require('../dig_pow.js');

describe.only('#digPow', function () {
  context('letters given', function () {
    it('assert something is equal', function () {
      assert.equal(1,1);
    });
  });
});

